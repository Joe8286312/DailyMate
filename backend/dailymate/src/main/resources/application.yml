spring:
  application:
    name: dailymate

  datasource:
    url: jdbc:mysql://localhost:3306/dailymate?useSSL=false&serverTimezone=UTC&characterEncoding=utf8
    username: root
    password: xiang123
    driver-class-name: com.mysql.cj.jdbc.Driver

  # JPA??
  jpa:
    hibernate:
      ddl-auto: update   # 'update'????????????????????
    show-sql: true
    properties:
      hibernate:
        format_sql: true

# ?????
server:
  port: 8080
