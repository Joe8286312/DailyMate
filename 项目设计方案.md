# DailyMate 项目完整设计方案

> 版本：v1.0 | 最后更新：2026-04-03

---

## 一、项目概述

### 1.1 项目名称
**DailyMate** - 个人日常管理工具

### 1.2 项目定位
一款全栈个人管理应用，提供待办事项管理、账单记账、日历视图、数据统计和智能提醒等功能，帮助用户高效管理日常事务。

### 1.3 核心价值
- ✅ **一站式管理**：待办事项 + 账单记账 + 数据统计
- 🌍 **多语言支持**：中文、英文、日文、韩文
- 🎨 **个性化定制**：5种主题 + 自定义背景图
- 🔔 **智能提醒**：SSE 实时推送 + 浏览器通知
- 📊 **数据可视化**：ECharts 图表分析

### 1.4 目标用户
- 个人用户：日常任务管理、财务记账
- 自由职业者：工作进度跟踪、收入支出统计
- 学生：学习计划管理、生活费用记录

---

## 二、技术架构

### 2.1 整体架构

```
┌─────────────────────────────────────────────────────┐
│                    用户浏览器                         │
└────────────────────┬────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────┐
│               Nginx (端口 80)                        │
│  - 前端静态资源服务                                   │
│  - 反向代理 /api → 后端服务                          │
└────────────────────┬────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────┐
│          前端应用 (Vue 3 SPA)                        │
│  - Vue 3.4 + Vite 5 + Element Plus 2.6              │
│  - Pinia 状态管理 + Vue Router 路由                  │
│  - Axios HTTP 客户端 + vue-i18n 国际化              │
│  - ECharts 数据可视化                               │
└────────────────────┬────────────────────────────────┘
                     │ HTTP/REST API (JSON)
                     ▼
┌─────────────────────────────────────────────────────┐
│          后端服务 (Spring Boot 3.2)                  │
│  - Spring Web MVC (RESTful API)                     │
│  - Spring Security + JWT 认证                       │
│  - Spring Data JPA 数据访问                         │
│  - SSE (Server-Sent Events) 实时推送                │
│  - @Scheduled 定时任务                              │
└────────────────────┬────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────┐
│          数据库 (MySQL 8.0)                          │
│  - 用户表、待办表、账单表、消息表、设置表             │
│  - 索引优化、UTF-8 编码、时区 +08:00                │
└─────────────────────────────────────────────────────┘
```

### 2.2 技术栈详细清单

#### 前端技术栈
| 技术 | 版本 | 用途 |
|------|------|------|
| Vue 3 | 3.4.21 | 核心框架（Composition API） |
| Vite | 5.2.8 | 构建工具 + 开发服务器 |
| Element Plus | 2.6.3 | UI 组件库 |
| Pinia | 2.1.7 | 状态管理（自定义持久化插件） |
| Vue Router | 4.3.0 | 前端路由 |
| Axios | 1.6.8 | HTTP 请求客户端 |
| vue-i18n | 9.14.5 | 国际化（4种语言） |
| ECharts | 6.0.0 | 数据可视化图表 |
| dayjs | 1.11.x | 日期时间处理 |
| Sass | 1.75.0 | CSS 预处理器 |
| nprogress | 0.2.0 | 页面加载进度条 |
| Terser | 5.46.1 | 代码压缩（生产环境移除 console） |

#### 后端技术栈
| 技术 | 版本 | 用途 |
|------|------|------|
| Spring Boot | 3.2.4 | 核心框架 |
| Spring Security | 3.2.4 | 安全框架 |
| Spring Data JPA | 3.2.4 | ORM 框架 |
| JWT (jjwt) | 0.11.5 | Token 认证 |
| MySQL Connector | 8.x | 数据库驱动 |
| Lombok | - | 代码简化（@Data 等注解） |
| SpringDoc OpenAPI | 2.3.0 | API 文档（Swagger） |
| HikariCP | - | 数据库连接池 |

#### 部署技术栈
| 技术 | 版本 | 用途 |
|------|------|------|
| Docker | - | 容器化 |
| Docker Compose | 3.8 | 多容器编排 |
| Nginx | - | Web 服务器 + 反向代理 |
| MySQL | 8.0 | 数据库 |

---

## 三、功能模块设计

### 3.1 用户认证模块

#### 功能清单
| 功能 | 接口 | 说明 |
|------|------|------|
| 用户注册 | POST /api/auth/register | 用户名+密码+邮箱（可选）+ 头像（可选） |
| 用户登录 | POST /api/auth/login | 返回 JWT Token + 用户信息 |
| 获取当前用户 | GET /api/user/me | 从 Token 解析用户ID |
| 修改个人资料 | PUT /api/user/profile | 邮箱、头像、用户名 |
| 修改密码 | PUT /api/user/password | 需验证旧密码 |
| 注销账号 | DELETE /api/user/me | 软删除（isDelete=1） |
| 退出登录 | POST /api/auth/logout | 前端清除 Token |

#### 安全设计
- **密码加密**：BCrypt 强哈希，禁止明文存储
- **Token 机制**：JWT Bearer Token，有效期 24 小时
- **认证拦截**：所有 API 需携带 Token（除登录注册）
- **路由守卫**：前端路由拦截，未登录跳转 `/login`

#### 数据流
```
用户输入 → 前端表单验证 → API 请求 → 后端参数校验 → 
密码加密(注册) → 数据库查询 → 生成 Token(登录) → 
返回结果 → 前端存储 Token 至 localStorage → 跳转首页
```

---

### 3.2 待办事项模块

#### 核心功能
| 功能 | 说明 |
|------|------|
| 添加待办 | 标题（必填）、内容、优先级、日期、开始/结束时间、提醒设置 |
| 编辑待办 | 修改任意字段，自动更新 `updatedAt` |
| 删除待办 | 软删除（isDelete=1）+ 硬删除（物理删除） |
| 恢复待办 | 恢复软删除的记录（isDelete=0） |
| 批量操作 | 批量完成、批量删除、批量硬删除 |
| 搜索筛选 | 关键字搜索、状态筛选、优先级筛选、日期区间查询 |
| 提醒功能 | SSE 实时推送 + 浏览器通知 + 声音提醒 |

#### 待办实体字段
```java
id: Long                 // 主键，自增
userId: Long             // 所属用户ID
title: String(200)       // 标题（必填）
content: String          // 详细描述（可选）
priority: Integer        // 优先级：1-高，2-中，3-低
status: Integer          // 状态：0-未完成，1-已完成
date: Date               // 归属日期（必填）
startTime: DateTime      // 开始时间（可选）
endTime: DateTime        // 截止时间（可选）
finishTime: DateTime     // 完成时间（status=1 时自动记录）
isDelete: Integer        // 软删除：0-否，1-是
reminderEnabled: Boolean // 是否启用提醒
reminderOffset: Integer  // 提前提醒时间（分钟）
reminderTime: DateTime   // 计算出的实际提醒时间
isReminded: Boolean      // 是否已提醒
createdAt: DateTime      // 创建时间（自动填充）
updatedAt: DateTime      // 更新时间（自动填充）
```

#### 提醒机制
1. **定时任务**：`@Scheduled` 每分钟扫描 `reminderTime <= now` 且 `isReminded=false` 的待办
2. **SSE 推送**：建立持久连接 `GET /api/todo/remind/stream/{userId}`
3. **浏览器通知**：前端调用 Web Notifications API
4. **声音提醒**：播放提示音（可关闭）
5. **标记已提醒**：更新 `isReminded=true`，避免重复推送

#### API 接口清单
| 接口 | 方法 | 说明 |
|------|------|------|
| /api/todo/list | GET | 获取列表（可选 date 参数） |
| /api/todo/add | POST | 添加待办 |
| /api/todo/update | PUT | 更新待办 |
| /api/todo/delete/{id} | DELETE | 软删除 |
| /api/todo/hard-delete/{id} | DELETE | 硬删除 |
| /api/todo/restore/{id} | PUT | 恢复软删除 |
| /api/todo/batch/finish | PUT | 批量完成 `{"ids":[1,2], "status":1}` |
| /api/todo/batch/delete | PUT | 批量软删除 `{"ids":[1,2]}` |
| /api/todo/batch/hard-delete | PUT | 批量硬删除 |
| /api/todo/search | GET | 关键字搜索 `keyword=xxx` |
| /api/todo/range | GET | 日期区间 `start=xxx&end=xxx` |
| /api/todo/by-status | GET | 按状态筛选 `status=0/1` |
| /api/todo/by-priority | GET | 按优先级筛选 `priority=1/2/3` |
| /api/todo/unfinished-count | GET | 未完成数量 |
| /api/todo/priority/{id} | PUT | 修改优先级 |
| /api/todo/remind/stream/{userId} | GET | SSE 提醒流 |
| /api/todo/remind/set/{id} | POST | 设置提醒 |
| /api/todo/remind/cancel/{id} | POST | 取消提醒 |
| /api/todo/remind/ack/{id} | POST | 确认收到提醒 |

---

### 3.3 账单管理模块

#### 核心功能
| 功能 | 说明 |
|------|------|
| 添加账单 | 类型（收入/支出）、分类、金额、日期、备注 |
| 编辑账单 | 修改任意字段 |
| 删除账单 | 软删除 + 硬删除 |
| 恢复账单 | 恢复软删除记录 |
| 批量删除 | 批量软删除、批量硬删除 |
| 搜索筛选 | 关键字、分类、金额区间、时间区间 |
| 统计分析 | 月度统计、分类统计、趋势分析 |

#### 账单实体字段
```java
id: Long                 // 主键
userId: Long             // 所属用户ID
type: Integer            // 类型：1-收入，2-支出
category: String(50)     // 分类（餐饮、购物、交通等）
amount: Decimal(10,2)    // 金额
remark: String           // 备注（可选）
date: Date               // 账单日期
isDelete: Integer        // 软删除标记
createdAt: DateTime      // 创建时间
updatedAt: DateTime      // 更新时间
```

#### 分类预设
- **支出分类**：餐饮、购物、交通、住房、娱乐、医疗、教育、其他
- **收入分类**：工资、兼职、投资、礼金、其他

#### 统计接口
| 接口 | 参数 | 返回 |
|------|------|------|
| /api/bill/stat/month | userId, year, month | `{income: 4200, expense: 830, total: 3370}` |
| /api/bill/stat/category | userId, type, start, end | `{"餐饮": 400, "购物": 430}` |
| /api/bill/stat/trend | userId, days | `[{date, income, expense, total}, ...]` |

#### API 接口清单
| 接口 | 方法 | 说明 |
|------|------|------|
| /api/bill/list | GET | 获取列表（可选 date 参数） |
| /api/bill/add | POST | 添加账单 |
| /api/bill/update | PUT | 更新账单 |
| /api/bill/delete/{id} | DELETE | 软删除 |
| /api/bill/hard-delete/{id} | DELETE | 硬删除 |
| /api/bill/restore/{id} | PUT | 恢复软删除 |
| /api/bill/batch/delete | PUT | 批量软删除 |
| /api/bill/batch/hard-delete | PUT | 批量硬删除 |
| /api/bill/range | GET | 时间区间查询 |
| /api/bill/by-category | GET | 按分类查询 |
| /api/bill/by-amount-range | GET | 按金额区间查询 |
| /api/bill/search | GET | 关键字搜索 |
| /api/bill/stat/month | GET | 月度统计 |
| /api/bill/stat/category | GET | 分类统计 |
| /api/bill/stat/trend | GET | 趋势统计 |

---

### 3.4 消息通知模块

#### 功能说明
| 功能 | 说明 |
|------|------|
| 消息列表 | 分页获取用户消息 |
| 未读数量 | 获取未读消息数量（显示在导航栏） |
| 标记已读 | 单条标记已读、批量标记已读 |
| 删除消息 | 删除单条、删除所有已读 |
| 消息类型 | 系统消息、待办提醒、账单提醒 |

#### 消息实体
```java
id: Long                 // 主键
userId: Long             // 所属用户ID
title: String(200)       // 消息标题
content: String          // 消息内容
type: Integer            // 类型：0-系统，1-待办，2-账单
isRead: Integer          // 已读：0-否，1-是
readTime: DateTime       // 阅读时间
createdAt: DateTime      // 创建时间
updatedAt: DateTime      // 更新时间
```

---

### 3.5 用户设置模块

#### 设置项清单
| 设置项 | 类型 | 说明 | 默认值 |
|--------|------|------|--------|
| language | String | 语言：zh-CN, en-US, ja-JP, ko-KR | zh-CN |
| theme | String | 主题：light, dark, blue, green, purple | light |
| backgroundType | String | 背景类型：theme, image | theme |
| backgroundUrl | String | 背景图 URL | null |
| backgroundOpacity | Integer | 背景透明度 0-100 | 100 |
| backgroundSize | String | 背景尺寸：cover, contain, 100%, auto 100% | cover |
| backgroundPosition | String | 背景位置：center, left top, right top 等 | center |
| notificationEnabled | Boolean | 是否启用浏览器通知 | false |
| soundEnabled | Boolean | 是否启用声音提醒 | true |
| animationEnabled | Boolean | 是否启用动画效果 | true |
| defaultPriority | Integer | 默认优先级：1-高，2-中，3-低 | 2 |
| defaultReminderOffset | Integer | 默认提醒提前时间（分钟） | 0 |

#### 主题预设
| 主题名 | 背景渐变 | 适用场景 |
|--------|----------|----------|
| light | #f5f7fa → #e4edf5 | 日间办公 |
| dark | #1a1a2e → #16213e | 夜间模式 |
| blue | #1e3a8a → #3b82f6 → #60a5fa | 科技风 |
| green | #064e3b → #10b981 → #34d399 | 清新自然 |
| purple | #581c87 → #8b5cf6 → #a78bfa | 浪漫优雅 |

#### 背景图功能
- **预设背景**：8 种预设背景图（自然风光、海洋、山川、日落、星空等）
- **自定义上传**：支持 Base64 图片上传（建议压缩后存储）
- **URL 输入**：输入外部图片链接
- **图片预览**：上传前预览效果
- **清除功能**：恢复默认背景

---

### 3.6 仪表盘模块

#### 展示内容
| 模块 | 说明 |
|------|------|
| 统计卡片 | 今日待办数、未完成数、今日收支、月度总支出 |
| 今日概览 | 今日待办列表（按时间排序） |
| 快捷操作 | 快速添加待办、快速添加账单 |
| 月度趋势 | ECharts 折线图展示近 7/30 天收支趋势 |
| 分类统计 | ECharts 饼图展示本月支出分类占比 |
| 完成进度 | 本周待办完成情况（进度条） |

---

## 四、数据库设计

### 4.1 表结构总览

| 表名 | 说明 | 记录数预估 |
|------|------|------------|
| user | 用户表 | 千级 |
| todo | 待办事项表 | 万级 |
| bill | 账单表 | 万级 |
| message | 消息表 | 万级 |
| user_settings | 用户设置表 | 千级（1:1 关联 user） |

### 4.2 核心表结构

#### user 表
```sql
CREATE TABLE `user` (
    `id` BIGINT NOT NULL AUTO_INCREMENT,
    `username` VARCHAR(50) NOT NULL UNIQUE,
    `password` VARCHAR(255) NOT NULL,
    `email` VARCHAR(100) DEFAULT NULL UNIQUE,
    `avatar` VARCHAR(255) DEFAULT NULL,
    `is_delete` TINYINT DEFAULT 0,
    `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
    `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    INDEX `idx_username` (`username`),
    INDEX `idx_email` (`email`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### todo 表
```sql
CREATE TABLE `todo` (
    `id` BIGINT NOT NULL AUTO_INCREMENT,
    `user_id` BIGINT NOT NULL,
    `title` VARCHAR(200) NOT NULL,
    `content` TEXT DEFAULT NULL,
    `priority` TINYINT DEFAULT 2,
    `status` TINYINT DEFAULT 0,
    `date` DATE NOT NULL,
    `start_time` DATETIME DEFAULT NULL,
    `end_time` DATETIME DEFAULT NULL,
    `finish_time` DATETIME DEFAULT NULL,
    `is_delete` TINYINT DEFAULT 0,
    `reminder_enabled` TINYINT DEFAULT 0,
    `reminder_offset` INT DEFAULT 0,
    `reminder_time` DATETIME DEFAULT NULL,
    `is_reminded` TINYINT DEFAULT 0,
    `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
    `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    INDEX `idx_user_id` (`user_id`),
    INDEX `idx_status` (`status`),
    INDEX `idx_date` (`date`),
    INDEX `idx_reminder` (`reminder_enabled`, `is_reminded`, `reminder_time`),
    INDEX `idx_user_status` (`user_id`, `status`),
    FOREIGN KEY (`user_id`) REFERENCES `user`(`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### bill 表
```sql
CREATE TABLE `bill` (
    `id` BIGINT NOT NULL AUTO_INCREMENT,
    `user_id` BIGINT NOT NULL,
    `type` TINYINT NOT NULL COMMENT '1-收入，2-支出',
    `category` VARCHAR(50) NOT NULL,
    `amount` DECIMAL(10,2) NOT NULL,
    `remark` VARCHAR(500) DEFAULT NULL,
    `date` DATE NOT NULL,
    `is_delete` TINYINT DEFAULT 0,
    `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
    `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    INDEX `idx_user_id` (`user_id`),
    INDEX `idx_type` (`type`),
    INDEX `idx_date` (`date`),
    INDEX `idx_category` (`category`),
    INDEX `idx_user_type` (`user_id`, `type`),
    FOREIGN KEY (`user_id`) REFERENCES `user`(`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### message 表
```sql
CREATE TABLE `message` (
    `id` BIGINT NOT NULL AUTO_INCREMENT,
    `user_id` BIGINT NOT NULL,
    `title` VARCHAR(200) NOT NULL,
    `content` TEXT DEFAULT NULL,
    `type` TINYINT DEFAULT 0 COMMENT '0-系统，1-待办，2-账单',
    `is_read` TINYINT DEFAULT 0,
    `read_time` DATETIME DEFAULT NULL,
    `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
    `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    INDEX `idx_user_id` (`user_id`),
    INDEX `idx_is_read` (`is_read`),
    INDEX `idx_user_is_read` (`user_id`, `is_read`),
    FOREIGN KEY (`user_id`) REFERENCES `user`(`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### user_settings 表
```sql
CREATE TABLE `user_settings` (
    `id` BIGINT NOT NULL AUTO_INCREMENT,
    `user_id` BIGINT NOT NULL UNIQUE,
    `language` VARCHAR(10) DEFAULT 'zh-CN',
    `theme` VARCHAR(20) DEFAULT 'light',
    `background_type` VARCHAR(20) DEFAULT 'theme',
    `background_url` TEXT DEFAULT NULL,
    `background_opacity` INT DEFAULT 100,
    `background_size` VARCHAR(20) DEFAULT 'cover',
    `background_position` VARCHAR(50) DEFAULT 'center',
    `notification_enabled` TINYINT DEFAULT 0,
    `sound_enabled` TINYINT DEFAULT 1,
    `animation_enabled` TINYINT DEFAULT 1,
    `default_priority` TINYINT DEFAULT 2,
    `default_reminder_offset` INT DEFAULT 0,
    `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
    `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    INDEX `idx_user_id` (`user_id`),
    FOREIGN KEY (`user_id`) REFERENCES `user`(`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

### 4.3 索引优化策略
- **高频查询字段**全部建立索引：user_id, status, date, type
- **组合索引**：(user_id, status)、(user_id, type)、(user_id, is_read)
- **提醒功能索引**：(reminder_enabled, is_reminded, reminder_time) 用于定时任务扫描
- **外键约束**：所有关联 user_id 的表均设置 ON DELETE CASCADE

---

## 五、前端架构设计

### 5.1 目录结构
```
frontend/
├── src/
│   ├── api/                 # API 接口封装
│   │   ├── auth.js          # 认证相关 API
│   │   ├── todo.js          # 待办相关 API
│   │   ├── bill.js          # 账单相关 API
│   │   ├── message.js       # 消息相关 API
│   │   └── settings.js      # 设置相关 API
│   ├── components/          # 公共组件
│   │   ├── BillStatistics.vue    # 账单统计图表组件
│   │   ├── MessageDrawer.vue     # 消息抽屉组件
│   │   └── NotificationSettings.vue  # 通知设置组件
│   ├── locales/             # 国际化配置
│   │   ├── index.js         # i18n 配置
│   │   ├── element-locale.js # Element Plus 语言映射
│   │   ├── zh-CN.js         # 简体中文
│   │   ├── en-US.js         # English
│   │   ├── ja-JP.js         # 日本語
│   │   └── ko-KR.js         # 한국어
│   ├── router/              # 路由配置
│   │   └── index.js         # 路由定义 + 守卫
│   ├── stores/              # Pinia 状态管理
│   │   ├── plugins/
│   │   │   └── persist.js   # 自定义持久化插件
│   │   ├── auth.js          # 认证状态
│   │   ├── todo.js          # 待办状态
│   │   ├── bill.js          # 账单状态
│   │   ├── message.js       # 消息状态
│   │   └── settings.js      # 设置状态（含主题配置）
│   ├── styles/              # 样式文件
│   │   ├── variables.scss   # SCSS 变量（主题色、间距等）
│   │   └── index.scss       # 全局样式（重置、工具类）
│   ├── utils/               # 工具函数
│   │   ├── request.js       # Axios 封装（拦截器、重试）
│   │   ├── errorHandler.js  # 全局错误处理
│   │   ├── notification.js  # Web Notifications API
│   │   └── imageCompressor.js  # 图片压缩工具
│   ├── views/               # 页面组件
│   │   ├── Login.vue        # 登录页
│   │   ├── Register.vue     # 注册页
│   │   ├── Layout.vue       # 主布局（侧边栏+顶部）
│   │   ├── Dashboard.vue    # 仪表盘
│   │   ├── Todos.vue        # 待办事项页
│   │   ├── Bills.vue        # 账单管理页
│   │   └── Settings.vue     # 设置页
│   ├── App.vue              # 根组件（背景管理+主题注入）
│   └── main.js              # 入口文件（初始化 Vue 实例）
├── .env                     # 环境变量
├── .env.development         # 开发环境变量
├── .env.production          # 生产环境变量（VITE_DROP_CONSOLE=true）
├── .eslintrc.cjs            # ESLint 配置
├── vite.config.js           # Vite 配置
├── package.json             # 依赖配置
├── Dockerfile               # Docker 构建文件
├── nginx.conf               # Nginx 配置
└── index.html               # 入口 HTML
```

### 5.2 状态管理设计

#### Pinia Store 结构
每个 Store 包含：
- **state**: 响应式状态数据
- **getters**: 计算属性（类似 Vuex getters）
- **actions**: 异步操作（API 调用、业务逻辑）

#### auth Store
```javascript
state: { token, user }
getters: { isLoggedIn, userInfo }
actions: { login(), register(), fetchUser(), logout(), updateProfile(), changePassword() }
```

#### todo Store
```javascript
state: { todos, loading, filters: { status, priority, keyword, date } }
getters: { filteredTodos, unfinishedCount, todayTodos }
actions: { fetchTodos(), addTodo(), updateTodo(), deleteTodo(), 
           batchFinish(), batchDelete(), setSearchFilters() }
```

#### bill Store
```javascript
state: { bills, loading, stats: { month, category, trend }, filters }
getters: { filteredBills, monthIncome, monthExpense }
actions: { fetchBills(), addBill(), updateBill(), deleteBill(), 
           fetchMonthStats(), fetchCategoryStats(), fetchTrend() }
```

#### settings Store
```javascript
state: { 
  settings: { language, theme, background*, notification* },
  themes: { light, dark, blue, green, purple },  // 主题配置
  presetBackgrounds: [...]  // 预设背景图 URL
}
getters: { currentThemeVars, currentBackground }
actions: { fetchSettings(), updateSettings(), applyTheme(), resetSettings() }
```

#### message Store
```javascript
state: { messages, unreadCount, loading }
getters: { unreadMessages }
actions: { fetchMessages(), fetchUnreadCount(), markAsRead(), batchMarkAsRead(), 
           deleteMessage(), deleteAllRead() }
```

### 5.3 路由设计

#### 路由结构
```
/login              → Login.vue            (无需认证)
/register           → Register.vue         (无需认证)
/                   → Layout.vue           (需认证)
  ├── /dashboard    → Dashboard.vue
  ├── /todos        → Todos.vue
  ├── /bills        → Bills.vue
  └── /settings     → Settings.vue
```

#### 路由守卫逻辑
```javascript
router.beforeEach((to, from) => {
  const token = localStorage.getItem('dailyMate_token')
  const isAuthenticated = !!token

  // 需要认证但未登录 → 跳转登录
  if (to.meta.requiresAuth && !isAuthenticated) return '/login'
  
  // 已登录访问登录/注册 → 跳转首页
  if ((to.name === 'Login' || to.name === 'Register') && isAuthenticated) return '/dashboard'
  
  return true
})
```

### 5.4 API 请求封装

#### Axios 实例配置
```javascript
baseURL: '/api'           // 开发环境代理到 http://localhost:8080
timeout: 15000            // 15 秒超时
headers: { 'Content-Type': 'application/json' }
```

#### 请求拦截器
```javascript
// 自动注入 Token
config.headers.Authorization = `Bearer ${localStorage.getItem('dailyMate_token')}`

// 修改类请求显示 Loading
if (['post', 'put', 'delete'].includes(config.method)) {
  ElLoading.service()
}
```

#### 响应拦截器
```javascript
// 统一错误处理
if (response.status === 401) {
  // Token 过期或无效 → 清除 Token → 跳转登录
  localStorage.removeItem('dailyMate_token')
  router.push('/login')
}

// 业务错误码处理
if (!response.data.success) {
  ElMessage.error(response.data.msg)
  return Promise.reject(new Error(response.data.msg))
}
```

### 5.5 国际化实现

#### 语言加载逻辑
```javascript
// 动态加载语言模块
const messages = {
  'zh-CN': () => import('./locales/zh-CN.js'),
  'en-US': () => import('./locales/en-US.js'),
  'ja-JP': () => import('./locales/ja-JP.js'),
  'ko-KR': () => import('./locales/ko-KR.js')
}

// Element Plus 组件语言同步
const elementLocaleMap = {
  'zh-CN': zhCn,
  'en-US': en,
  'ja-JP': ja,
  'ko-KR': ko
}
```

#### 语言切换流程
1. 用户在设置页选择语言
2. 更新 `settings.language` 并调用后端保存
3. 调用 `i18n.global.locale.value = newLocale`
4. 更新 Element Plus locale：`app.use(ElementPlus, { locale: elementLocaleMap[newLocale] })`
5. 存储至 `localStorage.dailyMate_language`

### 5.6 主题与背景管理

#### 主题应用机制
```javascript
// 在 <head> 中动态注入 CSS 变量
const style = document.createElement('style')
style.textContent = `
  :root {
    --primary-color: ${theme.primary};
    --bg-gradient: ${theme.background};
    --text-color: ${theme.text};
    // ... 更多变量
  }
`
document.head.appendChild(style)
```

#### 背景层实现 (App.vue)
```vue
<div class="app-background" :style="backgroundStyle">
  <div class="app-overlay" :style="{ opacity: settings.backgroundOpacity / 100 }"></div>
</div>
<router-view class="app-content" />
```

---

## 六、后端架构设计

### 6.1 目录结构
```
backend/dailymate/
├── src/main/java/com/joe/dailymate/
│   ├── common/              # 公共类
│   │   ├── Result.java      # 统一响应格式
│   │   └── GlobalExceptionHandler.java  # 全局异常处理
│   ├── config/              # 配置类
│   │   ├── CorsConfig.java  # CORS 跨域配置
│   │   ├── JwtConfig.java   # JWT 配置
│   │   ├── ScheduleConfig.java  # 定时任务配置
│   │   └── SecurityConfig.java  # Spring Security 配置
│   ├── controller/          # 控制器层
│   │   ├── AuthController.java
│   │   ├── UserController.java
│   │   ├── TodoController.java
│   │   ├── BillController.java
│   │   ├── MessageController.java
│   │   ├── SettingsController.java
│   │   └── AvatarController.java
│   ├── dto/                 # 数据传输对象
│   │   ├── request/         # 请求 DTO
│   │   │   ├── LoginRequest.java
│   │   │   ├── RegisterRequest.java
│   │   │   ├── TodoRequest.java
│   │   │   ├── BillRequest.java
│   │   │   └── UserSettingsRequest.java
│   │   └── response/        # 响应 DTO（可选）
│   ├── entity/              # 实体类
│   │   ├── User.java
│   │   ├── Todo.java
│   │   ├── Bill.java
│   │   ├── Message.java
│   │   └── UserSettings.java
│   ├── filter/              # 过滤器
│   │   └── JwtAuthenticationFilter.java  # JWT 认证过滤器
│   ├── repository/          # 数据访问层（JPA）
│   │   ├── UserRepository.java
│   │   ├── TodoRepository.java
│   │   ├── BillRepository.java
│   │   ├── MessageRepository.java
│   │   └── UserSettingsRepository.java
│   ├── service/             # 业务逻辑层
│   │   ├── AuthService.java
│   │   ├── UserService.java
│   │   ├── TodoService.java
│   │   ├── BillService.java
│   │   ├── MessageService.java
│   │   ├── UserSettingsService.java
│   │   └── ReminderService.java  # 提醒服务（含定时任务）
│   ├── util/                # 工具类
│   │   └── JwtUtil.java     # JWT 生成/验证
│   └── DailymateApplication.java  # 启动类（@EnableScheduling）
├── src/main/resources/
│   ├── application.yml      # 主配置文件（多环境）
│   └── logback-spring.xml   # 日志配置
├── Dockerfile               # Docker 构建文件
└── pom.xml                  # Maven 依赖配置
```

### 6.2 分层架构

#### Controller 层
- **职责**：接收 HTTP 请求、参数校验、调用 Service、返回响应
- **注解**：`@RestController`、`@RequestMapping`、`@Validated`
- **规范**：不包含业务逻辑，只处理请求和响应

```java
@PostMapping("/add")
public Result<Todo> addTodo(@Validated @RequestBody TodoRequest request) {
    Todo todo = todoService.addTodo(request);
    return Result.success(todo);
}
```

#### Service 层
- **职责**：业务逻辑处理、事务管理、调用 Repository
- **注解**：`@Service`、`@Transactional`
- **规范**：所有业务逻辑集中在此层

```java
@Transactional
public Todo addTodo(TodoRequest request) {
    Todo todo = convertToEntity(request);
    // 业务逻辑：计算 reminderTime
    if (todo.getReminderEnabled() && todo.getEndTime() != null) {
        Calendar cal = Calendar.getInstance();
        cal.setTime(todo.getEndTime());
        cal.add(Calendar.MINUTE, -todo.getReminderOffset());
        todo.setReminderTime(cal.getTime());
    }
    return todoRepository.save(todo);
}
```

#### Repository 层
- **职责**：数据库 CRUD、自定义查询方法
- **继承**：`JpaRepository<Entity, Long>`
- **规范**：方法命名遵循 Spring Data JPA 约定

```java
public interface TodoRepository extends JpaRepository<Todo, Long> {
    List<Todo> findByUserIdAndIsDelete(Long userId, Integer isDelete);
    List<Todo> findByUserIdAndDate(Long userId, Date date);
    List<Todo> findByReminderEnabledTrueAndIsRemindedFalse();
    Long countByUserIdAndStatusAndIsDelete(Long userId, Integer status, Integer isDelete);
}
```

### 6.3 统一响应格式

```java
@Data
public class Result<T> {
    private boolean success;
    private String msg;
    private T data;

    public static <T> Result<T> success(T data) {
        Result<T> result = new Result<>();
        result.setSuccess(true);
        result.setMsg("操作成功");
        result.setData(data);
        return result;
    }

    public static <T> Result<T> error(String msg) {
        Result<T> result = new Result<>();
        result.setSuccess(false);
        result.setMsg(msg);
        return result;
    }
}
```

### 6.4 安全配置

#### Spring Security 配置
```java
@Configuration
@EnableWebSecurity
public class SecurityConfig {
    @Bean
    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
        http
            .csrf(csrf -> csrf.disable())  // 禁用 CSRF（使用 JWT）
            .authorizeHttpRequests(auth -> auth
                .requestMatchers("/api/auth/**").permitAll()  // 登录注册放行
                .requestMatchers("/swagger-ui/**", "/v3/api-docs/**").permitAll()
                .anyRequest().authenticated()  // 其他接口需认证
            )
            .sessionManagement(session -> session
                .sessionCreationPolicy(SessionCreationPolicy.STATELESS))  // 无状态
            .addFilterBefore(jwtFilter, UsernamePasswordAuthenticationFilter.class);
        return http.build();
    }
}
```

#### JWT 认证流程
```
1. 用户登录 → 验证用户名密码 → 生成 JWT Token → 返回 Token
2. 后续请求 → 携带 Authorization: Bearer <token>
3. JwtAuthenticationFilter 拦截 → 解析 Token → 设置 SecurityContext
4. Controller 从 SecurityContext 获取当前用户 ID
```

### 6.5 提醒服务设计

#### 定时任务
```java
@Service
@EnableScheduling
public class ReminderService {
    @Autowired
    private TodoRepository todoRepository;
    @Autowired
    private SseEmitterManager sseEmitterManager;

    @Scheduled(fixedRate = 60000)  // 每分钟执行
    public void checkReminders() {
        Date now = new Date();
        List<Todo> dueTodos = todoRepository.findByReminderEnabledTrueAndIsRemindedFalse()
            .stream()
            .filter(todo -> todo.getReminderTime() != null && !todo.getReminderTime().after(now))
            .toList();

        for (Todo todo : dueTodos) {
            // 1. 发送 SSE 消息
            sseEmitterManager.sendToUser(todo.getUserId(), new ReminderMessage(todo));
            
            // 2. 创建消息记录
            messageService.createMessage(todo.getUserId(), "待办提醒", todo.getTitle());
            
            // 3. 标记已提醒
            todo.setIsReminded(true);
            todoRepository.save(todo);
        }
    }
}
```

#### SSE 连接管理
```java
@RestController
public class TodoController {
    private final Map<Long, SseEmitter> emitters = new ConcurrentHashMap<>();

    @GetMapping("/remind/stream/{userId}")
    public SseEmitter streamReminders(@PathVariable Long userId) {
        SseEmitter emitter = new SseEmitter(Long.MAX_VALUE);  // 长连接
        emitters.put(userId, emitter);

        emitter.onCompletion(() -> emitters.remove(userId));
        emitter.onTimeout(() -> emitters.remove(userId));

        return emitter;
    }
}
```

---

## 七、部署架构

### 7.1 Docker 部署

#### docker-compose.yml
```yaml
version: '3.8'

services:
  mysql:
    image: mysql:8.0
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
      MYSQL_DATABASE: dailymate
    volumes:
      - mysql-data:/var/lib/mysql
      - ./docs/mysql/init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      retries: 5

  backend:
    build: ./backend/dailymate
    ports:
      - "8080:8080"
    environment:
      SPRING_PROFILES_ACTIVE: prod
      DB_PASSWORD: ${DB_PASSWORD}
      JWT_SECRET: ${JWT_SECRET}
      SPRING_DATASOURCE_URL: jdbc:mysql://mysql:3306/dailymate
    depends_on:
      mysql:
        condition: service_healthy

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend

volumes:
  mysql-data:
```

#### 部署步骤
```bash
# 1. 配置环境变量
cp .env.example .env
# 编辑 .env 文件，修改 DB_PASSWORD 和 JWT_SECRET

# 2. 一键启动
docker-compose up -d

# 3. 查看日志
docker-compose logs -f

# 4. 停止服务
docker-compose down

# 5. 重新构建并启动
docker-compose up -d --build
```

### 7.2 本地开发

#### 环境要求
```
Node.js >= 18
JDK 17
MySQL 8.0
Maven 3.6+
```

#### 启动步骤
```bash
# 1. 启动 MySQL
mysql -u root -p
CREATE DATABASE dailymate DEFAULT CHARACTER SET utf8mb4;
source docs/mysql/init.sql

# 2. 启动后端
cd backend/dailymate
mvn spring-boot:run

# 3. 启动前端
cd frontend
npm install
npm run dev

# 4. 访问应用
# 前端：http://localhost:3000
# 后端：http://localhost:8080
# Swagger: http://localhost:8080/swagger-ui.html
```

---

## 八、开发规范

### 8.1 前端规范

#### 命名规范
```javascript
// 变量/函数：camelCase
const fetchTodos = async () => {}
const userName = 'Alice'

// 组件：PascalCase
<TodoList />
<UserProfile />

// 常量：UPPER_SNAKE_CASE
const MAX_RETRY_COUNT = 3
const API_BASE_URL = '/api'

// 文件名：camelCase 或 PascalCase（组件）
api/todo.js
views/Todos.vue
```

#### 组件开发规范
```vue
<script setup>
// 1. 导入（Vue API → 第三方库 → 本地模块 → 样式）
import { ref, computed, onMounted } from 'vue'
import { ElMessage } from 'element-plus'
import { useTodoStore } from '@/stores/todo'
import './styles.scss'

// 2. Props 定义
const props = defineProps({
  todoId: { type: Number, required: true }
})

// 3. Emits 定义
const emit = defineEmits(['update', 'delete'])

// 4. 响应式状态
const loading = ref(false)
const todos = ref([])

// 5. 计算属性
const filteredTodos = computed(() => todos.value.filter(t => t.status === 0))

// 6. 方法
const fetchTodos = async () => {
  loading.value = true
  try {
    todos.value = await todoStore.fetchTodos()
  } finally {
    loading.value = false
  }
}

// 7. 生命周期
onMounted(() => {
  fetchTodos()
})
</script>

<template>
  <div class="todo-list">
    <!-- 模板内容 -->
  </div>
</template>

<style scoped lang="scss">
.todo-list {
  // 样式
}
</style>
```

### 8.2 后端规范

#### 命名规范
```java
// 类：PascalCase
public class TodoService {}

// 方法/变量：camelCase
public void addTodo() {}
private String userName;

// 常量：UPPER_SNAKE_CASE
private static final int MAX_RETRY = 3;

// 数据库字段：snake_case
private Long userId;  // 对应 user_id 列
```

#### Controller 规范
```java
@RestController
@RequestMapping("/api/todo")
@Validated
public class TodoController {
    
    @Autowired
    private TodoService todoService;

    @PostMapping("/add")
    public Result<Todo> addTodo(@Validated @RequestBody TodoRequest request) {
        return Result.success(todoService.addTodo(request));
    }
}
```

### 8.3 Git 提交规范

#### Commit Message 格式
```
<type>(<scope>): <subject>

<body>

<footer>
```

#### Type 类型
- `feat`: 新功能
- `fix`: 修复 Bug
- `docs`: 文档更新
- `style`: 代码格式（不影响功能）
- `refactor`: 重构
- `perf`: 性能优化
- `test`: 测试相关
- `chore`: 构建/工具链相关

#### 示例
```
feat(todo): 添加提醒功能

- 新增 reminderEnabled、reminderOffset 等字段
- 实现 SSE 实时推送
- 添加定时任务检查提醒

Closes #123
```

---

## 九、性能优化

### 9.1 前端优化

#### 构建优化
- **代码分割**：vendor-vue、vendor-element、vendor-utils 分包
- **Tree Shaking**：移除未使用代码
- **Terser 压缩**：生产环境移除 console.log
- **懒加载**：路由组件按需加载 `() => import()`
- **资源缓存**：静态资源添加 hash 文件名

#### 运行时优化
- **虚拟列表**：大数据量表格使用虚拟滚动
- **防抖/节流**：搜索输入、滚动事件
- **缓存策略**：Pinia 持久化，避免重复请求
- **图片压缩**：上传前压缩图片（imageCompressor.js）

### 9.2 后端优化

#### 数据库优化
- **索引优化**：高频查询字段建立索引
- **连接池**：HikariCP，最大连接数 20（生产）
- **批量操作**：使用 `saveAll()` 而非循环 `save()`

#### 接口优化
- **分页查询**：避免一次性加载大量数据
- **缓存策略**：统计数据缓存（如月度统计）
- **异步处理**：耗时操作使用 `@Async`

---

## 十、安全设计

### 10.1 前端安全
- **XSS 防护**：Vue 自动转义，禁止使用 `v-html` 渲染不可信内容
- **CSRF 防护**：使用 JWT Token，无需 CSRF Token
- **敏感数据存储**：Token 存储在 localStorage（注意 XSS 风险）
- **输入校验**：前端表单验证 + 后端参数校验双重保障

### 10.2 后端安全
- **密码加密**：BCrypt 强哈希，禁止明文存储
- **SQL 注入防护**：JPA 参数化查询
- **JWT 安全**：
  - 密钥长度 >= 32 字符
  - Token 有效期 24 小时
  - 生产环境必须使用环境变量配置密钥
- **CORS 配置**：生产环境限制允许的域名
- **接口限流**：可集成 Spring Cloud Gateway 实现限流

### 10.3 部署安全
- **环境变量**：敏感配置通过 `.env` 文件管理，禁止提交到 Git
- **容器安全**：使用非 root 用户运行容器
- **HTTPS**：生产环境必须启用 HTTPS
- **日志脱敏**：禁止在日志中记录密码、Token 等敏感信息

---

## 十一、测试策略

### 11.1 前端测试
- **单元测试**：Vitest + Vue Test Utils
- **E2E 测试**：Cypress
- **测试覆盖率**：关键业务逻辑覆盖率 >= 80%

### 11.2 后端测试
- **单元测试**：JUnit 5 + Mockito
- **集成测试**：`@SpringBootTest` + TestContainers
- **API 测试**：Postman / Swagger UI

---

## 十二、监控与日志

### 12.1 日志配置
```yaml
# 开发环境
logging:
  level:
    root: INFO
    com.joe.dailymate: DEBUG
  pattern:
    console: '%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n'

# 生产环境
logging:
  level:
    root: WARN
    com.joe.dailymate: INFO
  file:
    name: logs/dailymate.log
    max-size: 10MB
    max-history: 30
```

### 12.2 错误处理
- **前端**：全局错误捕获（errorHandler.js），统一错误提示
- **后端**：`@ControllerAdvice` + `@ExceptionHandler` 全局异常处理

---

## 十三、未来扩展

### 13.1 高优先级功能
- [ ] **待办重复规则**：每日/每周/每月重复
- [ ] **账单预算功能**：设置月度预算，超支预警
- [ ] **数据导出**：Excel/CSV 导出待办和账单

### 13.2 中优先级功能
- [ ] **番茄钟**：25 分钟专注 + 5 分钟休息
- [ ] **习惯打卡**：每日习惯追踪
- [ ] **数据看板**：更丰富的统计图表

### 13.3 低优先级功能
- [ ] **四象限视图**：按重要/紧急分类待办
- [ ] **协作功能**：共享待办清单
- [ ] **移动端适配**：响应式布局优化

---

## 十四、常见问题

### Q1: Token 过期如何处理？
**A**: 前端响应拦截器检测到 401 状态码，清除 Token 并跳转 `/login`。可扩展实现 Refresh Token 机制。

### Q2: 大图片背景如何优化？
**A**: 使用 `imageCompressor.js` 压缩图片，推荐尺寸 <= 1920x1080，文件大小 <= 500KB。

### Q3: SSE 连接断开如何重连？
**A**: 前端实现指数退避重连策略：1s → 2s → 4s → 8s → 16s，最大重连 5 次。

### Q4: 数据库性能瓶颈？
**A**: 
- 单表数据超过 100 万行时考虑分表
- 复杂查询使用 `EXPLAIN` 分析执行计划
- 定期清理软删除的历史数据

---

## 十五、项目清单

### 已完成功能
- ✅ 用户注册/登录/注销
- ✅ 待办事项 CRUD + 批量操作
- ✅ 账单管理 CRUD + 批量操作
- ✅ 待办提醒（SSE + 浏览器通知）
- ✅ 多语言支持（4 种语言）
- ✅ 主题切换（5 种主题）
- ✅ 自定义背景图
- ✅ 用户设置持久化
- ✅ 消息通知系统
- ✅ 数据统计（月度/分类/趋势）
- ✅ Docker 一键部署

### 已知问题
- ⚠️ Element Plus 组件语言切换后需刷新页面才能完全同步
- ⚠️ Base64 图片存储可能导致 localStorage 超限
- ⚠️ 大量并发提醒时 SSE 连接可能断开

---

## 十六、联系方式

- **项目仓库**: `D:\html\web_work\todo\DailyMate`
- **技术栈**: Vue 3 + Spring Boot + MySQL
- **文档日期**: 2026-04-03

---

**文档版本**: v1.0  
**最后更新**: 2026-04-03  
**维护者**: DailyMate Team
